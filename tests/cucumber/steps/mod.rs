//! Step definitions, organized by domain.
//!
//! Add a module per resource as features are written. Step functions use the
//! `#[given]` / `#[when]` / `#[then]` macros and operate on `TestWorld`.

pub mod items;
pub mod monitoring;
