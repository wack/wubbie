//! Training loop.
//!
//! The training pipeline (data loading, the Burn `Learner`, checkpointing) is
//! implemented in a follow-up ticket. It will run on
//! [`TrainBackend`](crate::backend::TrainBackend).
